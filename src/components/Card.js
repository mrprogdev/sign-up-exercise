import "./Card.css";

export const Card = (props) => {
  return <div className="card">{props.children}</div>;
};

// function Card(props) {
//   return <div className="card">{props.children}</div>;
// }

// export default Card;
